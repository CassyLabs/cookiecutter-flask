machine:
  python:
    version: 3.6
  post:
    - pyenv global 3.4 3.5 3.6
  node:
    version: 6.10

dependencies:
  pre:
    - pip install cookiecutter
    - pip install invoke==0.14.0
    - pip install pipenv
